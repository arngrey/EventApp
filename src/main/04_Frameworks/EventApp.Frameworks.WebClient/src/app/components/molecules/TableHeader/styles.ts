import styled from "styled-components";

export const TableHeaderContainer = styled.div`
    height: 100%;
    width: 100%;
    display: flex;
    flex-direction: row;
    position: relative;
    font-weight: 600;
`